/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
          {
            protocol: 'https',
            hostname: '4v2de31mgu.ufs.sh',
           
          },
        ],
      },
};

export default nextConfig;
